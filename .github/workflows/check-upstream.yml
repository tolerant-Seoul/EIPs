name: Check Upstream EIPs

on:
  schedule:
    # 매주 월요일 오전 9시 (KST) 실행
    - cron: '0 0 * * 1'
  workflow_dispatch:  # 수동 실행 가능

jobs:
  check-upstream:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Add upstream remote
        run: |
          git remote add upstream https://github.com/ethereum/EIPs.git || true
          git fetch upstream

      - name: Check for new EIPs
        id: check
        run: |
          echo "## 📋 Upstream 동기화 상태" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # 새로운/변경된 EIP 파일 확인
          NEW_EIPS=$(git diff HEAD upstream/master --name-only -- "EIPS/eip-*.md" | head -20)

          if [ -n "$NEW_EIPS" ]; then
            echo "### 🆕 새로운/변경된 EIP" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "$NEW_EIPS" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "new_eips=true" >> $GITHUB_OUTPUT
          else
            echo "✅ upstream과 동기화 상태입니다." >> $GITHUB_STEP_SUMMARY
            echo "new_eips=false" >> $GITHUB_OUTPUT
          fi

          # 번역 현황
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 번역 현황" >> $GITHUB_STEP_SUMMARY
          TRANSLATED=$(ls -1 EIPS/ko/eip-*.md 2>/dev/null | wc -l)
          TOTAL_FINAL=$(grep -l "^status: Final" EIPS/eip-*.md 2>/dev/null | wc -l)
          echo "- 번역 완료: ${TRANSLATED}개" >> $GITHUB_STEP_SUMMARY
          echo "- 전체 Final EIP: ${TOTAL_FINAL}개" >> $GITHUB_STEP_SUMMARY

      - name: Create issue if new EIPs found
        if: steps.check.outputs.new_eips == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const { execSync } = require('child_process');
            const newEips = execSync('git diff HEAD upstream/master --name-only -- "EIPS/eip-*.md" | head -10').toString().trim();

            const issueTitle = `[자동] Upstream에 새로운 EIP가 추가되었습니다`;
            const issueBody = `## 🆕 새로운/변경된 EIP 감지

            다음 EIP 파일들이 upstream에서 변경되었습니다:

            \`\`\`
            ${newEips}
            \`\`\`

            ### 권장 조치
            1. \`git fetch upstream && git merge upstream/master\` 실행
            2. 새로운 Final 상태의 중요 EIP 번역 검토
            3. 번역 완료 후 README.md 업데이트

            ---
            *이 Issue는 GitHub Actions에 의해 자동 생성되었습니다.*
            `;

            // 같은 제목의 열린 이슈가 있는지 확인
            const issues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'upstream-sync'
            });

            const existingIssue = issues.data.find(issue => issue.title === issueTitle);

            if (!existingIssue) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: issueTitle,
                body: issueBody,
                labels: ['upstream-sync', 'translation']
              });
            }
